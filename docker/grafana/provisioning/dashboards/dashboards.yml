apiVersion: 1

providers:
  - name: 'GPU Extraction Dashboards'
    orgId: 1
    folder: 'GPU Extraction'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
